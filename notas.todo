Hook de efecto:
    ☐ Se ejecuta cuando el componente ya fue montado, primero se ejecuta el componente y despues el Hook
    ☐ Al detectar que la variable cambia, el hook lo detecta y reacciona, en el ejemplo hace un log en consola mostrando cuantas veces se presiono el boton,
        tambien actualiza la vista