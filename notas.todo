Destructuring:
    ☐ Para evitar acceder a variables con mucha profundidad en un array, se utiliza el Destructuring
    ☐ Esto es, dentro de un array ponemos las propiedades de nuestro "props" principal
    ☐ Una vez transpiladas, podemos usarlas como variables normales, por ejemplo
    const { nombre, edad } = props;
    return 'Hola ' + nombre
    La funcion de arriba es mas sencilla que usar nombre como "props.nombre" y es mas legible
