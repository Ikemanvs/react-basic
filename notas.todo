Como se usan las variables en una funcion recibida de otro componente?:
    ☐ Si a la funcion recibida de otro componente se le pasa una variable, la funcion se ejecutara y no esperará al evento asignado (por ejemplo, al onClick)
    <button onClick={props.saludarFn(props.userInfo.nombre)}>
    ☐ Al llegar a los parentesis de la funcion "saludarFn" se ejecutara automaticamente dicha funcion, sin respetar el evento

    ☐ Es necesario poner dentro de la ejecución de la funcion una arrow function (funcion anonima)
    <button onClick={ () => props.saludarFn(props.userInfo.nombre)}>
    ☐ De esta manera, se espera que se ejecute el evento asignado